name: my first workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps: 
        -   name: Checkout Repo
            uses: actions/checkout@v4

        -   name: wlcome message
            run: echo "my first Github Actions"
        
        -   name: list filrs
            run: ls

        -   name: read files
            run: cat saivasanth26/action-1/README.md
        
